global:
  scrape_interval: 60s
  evaluation_interval: 60s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: rustchain_exporter
    static_configs:
      - targets:
          - rustchain-exporter:9100
